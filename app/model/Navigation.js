Ext.define('CfaSika.model.Navigation', {
    extend: 'Ext.data.Model',
    requires: [],

    config: {
        fields: [
            'url',
            'imageData',
            'title'
        ]
    }
});